server:
  port: 8761

spring:
  application:
    name: discovery-service

eureka:
  client:
    register-with-eureka: false   # <--- Tắt đăng ký chính nó
    fetch-registry: false         # <--- Không fetch danh sách service khác
